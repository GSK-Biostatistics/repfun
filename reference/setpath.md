# Set the current working directory.

Set the current working directory so that relative paths work as
expected.

## Usage

``` r
setpath(p)
```

## Arguments

- p:

  A path as a string.

## Value

'NULL' because the current working directory is set.

## Author

Yongwei Wang, <yongwei.x.wang@viivhealthcare.com>  
Chris Rook, <cr883296@gmail.com>

## Examples

``` r
if (FALSE) { # \dontrun{
library(repfun)
repfun::setpath()
} # }
```
